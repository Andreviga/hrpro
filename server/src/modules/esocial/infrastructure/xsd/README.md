# eSocial XSD Directory

Store official eSocial XSD files in versioned folders, for example:

- `S-1.0/`
- `S-1.1/`
- `S-1.2/`

Validation is optional and best-effort. The parser selects schema by event type, document type and layout version when available.
